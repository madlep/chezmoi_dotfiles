require("litee.lib").setup({})
require("litee.gh").setup({
    icon_set = "nerd",
    -- icon_set_custom = {
    --   ["Account"] = "x", -- author; commenter; assigned
    --   ["Bookmark"] = "x", -- PR label; issue label
    --   ["Calendar"] = "x", -- created at; last updated; comment date
    --   ["Check"] = "x", -- selected notification
    --   ["CheckAll"] = "x", -- all comments in thread resolved
    --   ["Circle"] = "x", -- read notification
    --   ["CircleFilled"] = "x", -- review requested; unread notification
    --   ["CirclePause"] = "x", -- check queued
    --   ["CircleSlash"] = "x", -- check skipped
    --   ["CircleStop"] = "x", -- check failure
    --   ["Comment"] = "x", -- comment; comment sign; comment outdated sign; comment resolved sign; comment pending sign
    --   ["DiffAdded"] = "x", -- comment can be added sign
    --   ["File"] = "x", -- file; path to file; line in file
    --   ["Folder"] = "x", -- file dir
    --   ["GitBranch"] = "x", -- base branch; head branch
    --   ["GitIssue"] = "x", -- issue
    --   ["GitCommit"] = "x", -- commit
    --   ["GitMerge"] = "x", -- pull request state merged
    --   ["GitPullRequest"] = "x", -- pull request; pull request state open
    --   ["GitRepo"] = "x", -- repo
    --   ["History"] = "x", -- thread is outdated
    --   ["Info"] = "x", -- check misc, pull request state
    --   ["MultiComment"] = "x", --  some comments in thread not resolved; comment thread; multiple comments on line
    --   ["Notification"] = "x", -- notification
    --   ["Number"] = "x", -- pull request number; notifications; issue number
    --   ["PassFilled"] = "x", -- review approved; check success
    --   ["Pencil"] = "x", -- review pending; comment pending; title; pull request state draft; issue title
    --   ["RequestChanges"] = "x", -- review request changes
    --   ["Sync"] = "x", -- check in progress
    -- }
})
